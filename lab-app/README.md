# 微纳气泡实验室移动 App

基于 React Native (Expo) 开发的跨平台移动应用，同时支持 iOS、Android 和鸿蒙系统。

## 功能特性

- 📱 **首页** - 实验室简介、研究方向概览、新闻动态
- 👥 **团队成员** - PI、教师、学生列表及个人简介
- 📄 **发表论文** - 论文列表（按年份分类）
- 🔬 **研究方向** - 5大研究方向详情
- 📰 **新闻动态** - 实验室最新资讯

## 技术栈

- **框架**: React Native + Expo SDK 52
- **语言**: TypeScript
- **导航**: React Navigation (Bottom Tabs)
- **目标平台**: iOS, Android, 鸿蒙

## 快速开始

### 前置要求

- Node.js 18+
- npm 或 yarn

### 安装依赖

```bash
cd lab-app
npm install
```

### 启动开发服务器

```bash
# 启动 Metro bundler
npm start

# 或者直接运行
npm run android  # Android
npm run ios      # iOS
```

### 构建 APK

```bash
# Android
npm run android

# iOS (需要 macOS)
npm run ios
```

## 项目结构

```
lab-app/
├── src/
│   ├── App.tsx           # 应用入口和导航配置
│   ├── theme.ts          # 主题配置（颜色、间距、字体）
│   ├── data/             # 数据模型
│   │   ├── people.ts    # 团队成员数据
│   │   ├── publications.ts # 论文数据
│   │   ├── research.ts  # 研究方向数据
│   │   └── news.ts      # 新闻数据
│   └── screens/          # 页面组件
│       ├── HomeScreen.tsx
│       ├── PeopleScreen.tsx
│       ├── PublicationsScreen.tsx
│       ├── ResearchScreen.tsx
│       └── NewsScreen.tsx
├── assets/               # 静态资源
├── app.json              # Expo 配置
├── package.json          # 项目依赖
└── tsconfig.json        # TypeScript 配置
```

## 数据同步

App 数据从网站项目的 `src/data/` 目录同步而来，确保内容一致。

## 许可证

MIT License
